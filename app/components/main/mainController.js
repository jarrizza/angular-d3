"use strict";

/* This controller manages variables at the highest scope of the application  */

app.controller('MainCtrl',[
    '$scope',
    '$location',
    '$interval',
    function ($scope, $location, $interval) {
        var vmMain = this;

        // This is where variables and functions that are "global" to main are placed
    }
]);